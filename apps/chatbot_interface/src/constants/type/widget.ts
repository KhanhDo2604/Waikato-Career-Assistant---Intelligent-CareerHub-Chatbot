export type FloatingButtonProps = {
    modalIsOpen?: boolean;
    onClick: () => void;
};

export type InformationCardProps = {
    title: string;
    description?: string;
    onClick?: () => void;
};
